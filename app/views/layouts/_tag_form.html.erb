
 <%= simple_form_for(@cosmetic) do |f| %>
  <%= f.input :tag_list, collection: Cosmetic::TAGS, input_html: { data: { controller: 'select2', select2_options_value: {tags: true} }} %>
  <%= f.submit %>
 <% end %>

