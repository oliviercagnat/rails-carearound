
// Select the review creation form with querySelector

// display none form(d-none)

// add a list of cosmetic reviews in cosmetic showpage (In HTML page this is not JS)

// when you have the list you want to select it with querySelector

// You want to insert adjasentHTML the new review => this will go inside the list of review

console.log('This is the review:'  + "<%= @review.content %>")
