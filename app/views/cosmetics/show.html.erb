
<div class="container">
  <div class="col-12">
    <div class="cosme-card">
      <div class="cosme-card-header">
        <%= image_tag @cosmetic.cosmetic_image, class: "img"%>
      </div>
      <div class="cosme-card-body">
        <span class="tag tag-teal"><%= @cosmetic.category.capitalize %></span>
        <h3> <%= @cosmetic.name %>
        </h3>
        <h4>from <%= @cosmetic.brand.capitalize %></h4>
        <p>
          <%= @cosmetic.description %>
        </p>
          <div class="price-info">
            <h5>Average price</h5>
            <small>¥<%= @cosmetic.average_price %></small>
          </div>
          <br>
          <p>Please type your tags seperated by a comma.</p>
          <% if policy(Cosmetic).update? %>
            <%= render 'layouts/tag_form' %>
              <% @cosmetic.tag_list.each do |tag| %>
                <li class= "tag tag-teal"><%= tag %></li>
              <% end %>
            </span>
          <% end %>
      </div>
    </div>
  </div>
</div>
